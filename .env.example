# Optional — wird für absolute URLs in Metadata, robots.txt und sitemap.xml verwendet.
# Auf Vercel automatisch via process.env.VERCEL_URL gefüllt, lokaler Fallback ist hausundhang.li.
# Setze diese Variable, wenn du eine eigene Domain konfigurierst (z. B. hausundhang.li).
NEXT_PUBLIC_SITE_URL=https://hausundhang.li
