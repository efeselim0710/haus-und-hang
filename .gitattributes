# Git LFS — Hero-Frame-Sequence (~85 MB, 435 Frames).
# Aktiv, sobald `git lfs install` einmalig auf dieser Maschine ausgeführt wurde.
# Ohne LFS greift diese Regel nicht — Frames landen dann normal in der Git-History
# (funktioniert, Repo wird halt grösser).
